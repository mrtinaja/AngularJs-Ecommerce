import { ConcatenarTextoPipe } from './concatenar-texto.pipe';

describe('ConcatenarTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatenarTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
